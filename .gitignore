/target/
# Nested Rust build dirs (e.g. apps/linux-tray/target)
**/target/
**/*.rs.bk
.DS_Store
*.swp
*.swo
.idea/
.vscode/
proptest-regressions/

# Build artifacts
build/
.gradle/
.kotlin/
local.properties
gradle_output.txt
*.class
node_modules/
# Per-machine Gradle daemon JVM pin
apps/android/gradle/gradle-daemon-jvm.properties

# Release binaries — go to GitHub releases, not repo
*.apk
*.dmg

# Tauri sidecar — rebuilt locally / shipped in the .app bundle, not the repo
apps/macos-tray/src-tauri/binaries/

# Dev scratch
crates/fluxsyncd/scratch/
# French design proto — internal scratch, not for the public repo
design-preview.html
# Marketing scratch (promo video / renders) — kept local
promo/

# Agent tooling — local knowledge base / locks, never shared
.agents/
.claude/scheduled_tasks.lock

design/chats/
# French design mockups — internal scratch, not for the public repo
design/project/
PROMPT-BACKEND-CLAUDE-CODE.md

# Agent tooling — not part of the public repo
.claude/
fluxsync-skill/

# Personal project memory + design handoff replica — local only
CLAUDE.local.md
fluxsync-ui-replica.html
# Design iteration scratch — local only
design-refined.html
design-boards/
claude-oss-application.html
